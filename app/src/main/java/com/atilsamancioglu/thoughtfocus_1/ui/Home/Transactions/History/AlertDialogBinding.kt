package com.atilsamancioglu.thoughtfocus_1.ui.Home.Transactions.History

import android.view.View
import android.widget.ImageView
import com.atilsamancioglu.thoughtfocus_1.R

class AlertDialogBinding(view: View) {
    val imageViewView: ImageView = view.findViewById(R.id.imageViewView)

    // You can add other views here if needed
}

//@BindingAdapter("loadGif")
//private fun loadGif(imageView: ImageView, gifResId: Int) {
//    Glide.with(imageView.context).asGif().load(gifResId).into(imageView)
//}
